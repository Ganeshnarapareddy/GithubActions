# name: First Workflow

# on:
#   push:

# jobs:
#   hello-job:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Print Message
#         run: echo "Hello GitHub Actions"

name: first workflow
on:
  push:
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Print Message
        run: echo "Hi This is my FIrst run"
    
